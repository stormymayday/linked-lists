import SinglyLinkedList from "@/implementations/singly-linked-list-scott";

export default function Home() {
    SinglyLinkedList();

    return (
        <main>
            <h1>Linked Lists</h1>
        </main>
    );
}
